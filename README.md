# Утилита голосового ввода

Утилита для голосового ввода текста в Windows с использованием офлайн распознавания речи Vosk.

## Возможности

- 🎤 Захват аудио с микрофона в реальном времени
- 🗣️ Распознавание русской речи через Vosk (офлайн)
- ⌨️ Автоматический ввод распознанного текста в активное окно
- 🎯 Голосовые команды для пунктуации (запятая, точка, новая строка и т.д.)
- 🔥 Глобальные горячие клавиши для управления
- 📱 Иконка в системном трее для быстрого доступа
- ⚙️ Настраиваемые параметры через config.json

## Требования

- Windows 10/11
- Python 3.8 или выше
- Микрофон
- Модель Vosk для русского языка

## Установка

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd audio
```

### 2. Установка зависимостей

```bash
pip install -r requirements.txt
```

**Примечание:** Для установки PyAudio на Windows может потребоваться:
- Скачать соответствующий wheel файл с https://www.lfd.uci.edu/~gohlke/pythonlibs/#pyaudio
- Или использовать: `pip install pipwin && pipwin install pyaudio`

### 3. Установка модели Vosk

1. Скачайте модель Vosk для русского языка:
   - Рекомендуется: [vosk-model-small-ru-0.22](https://alphacephei.com/vosk/models)
   - Или более точная: [vosk-model-ru-0.22](https://alphacephei.com/vosk/models)

2. Распакуйте модель в папку `models/`:
   ```
   models/
   └── vosk-model-small-ru-0.22/
       ├── am/
       ├── graph/
       └── ...
   ```

3. Убедитесь, что путь в `config.json` соответствует имени папки модели:
   ```json
   "vosk": {
     "model_path": "models/vosk-model-small-ru-0.22"
   }
   ```

## Настройка

Все настройки находятся в файле `config.json`:

### Параметры аудио

```json
"audio": {
  "sample_rate": 16000,  // Частота дискретизации (Гц)
  "chunk_size": 4000,    // Размер чанка
  "channels": 1          // Количество каналов (1 = моно)
}
```

### Настройки Vosk

```json
"vosk": {
  "model_path": "models/vosk-model-small-ru-0.22",
  "language": "ru",
  "words": true,           // Включать информацию о словах в результатах (SetWords)
  "partial_words": true   // Включать информацию о словах в частичных результатах (SetPartialWords)
}
```

**Примечание:** Согласно [документации Vosk](https://alphacephei.com/vosk/index.ru), эти параметры позволяют получать более детальную информацию о распознавании. `SetPartialWords` доступен в новых версиях библиотеки.

### Горячие клавиши

```json
"hotkeys": {
  "toggle": "win+h",          // Включить/выключить
  "pause": "ctrl+shift+p"     // Пауза/возобновление
}
```

### Метод ввода текста

```json
"input": {
  "method": "clipboard"       // clipboard или typing
}
```

- `clipboard` — текст копируется в буфер обмена и вставляется комбинацией `Ctrl+V` (по умолчанию, более надежно).
- `typing` — текст набирается посимвольно (полезно, если вставка из буфера заблокирована).

**Примечание:** `Win+H` может конфликтовать с системной функцией "Диктовка" в Windows 10/11. Если возникнут проблемы, используйте другую комбинацию (например, `ctrl+shift+v`).

### Голосовые команды

```json
"voice_commands": {
  "запятая": ",",
  "точка": ".",
  "восклицательный знак": "!",
  "вопросительный знак": "?",
  "двоеточие": ":",
  "точка с запятой": ";",
  "новая строка": "\n",
  "абзац": "\n\n",
  "пробел": " "
}
```

Вы можете добавить свои команды или изменить существующие.

## Использование

### Запуск приложения

```bash
python src/main.py
```

Или через entry point (после установки):

```bash
voice-input
```

### Управление

1. **Включение/выключение:**
   - Горячая клавиша: `Win+H` (по умолчанию)
   - Или через контекстное меню иконки в трее

2. **Пауза:**
   - Горячая клавиша: `Ctrl+Shift+P` (по умолчанию)

3. **Выход:**
   - Через контекстное меню иконки в трее → "Выход"

4. **Настройки горячих клавиш и метода ввода:**
   - Через контекстное меню иконки в трее → "Настройки"
   - Во всплывающем окне можно изменить комбинации и выбрать `clipboard`/`typing`

### Использование голосовых команд

Просто произнесите команду вместо знака препинания:
- "запятая" → `,`
- "точка" → `.`
- "новая строка" → перенос строки
- "абзац" → двойной перенос строки

## Сборка исполняемого файла

Для создания standalone исполняемого файла используйте PyInstaller:

```bash
pip install pyinstaller
pyinstaller build_exe.spec
```

Исполняемый файл будет создан в папке `dist/VoiceInput.exe`.

## Устранение неполадок

### Проблемы с PyAudio

Если возникают проблемы с установкой PyAudio:

1. Установите Microsoft Visual C++ Build Tools
2. Или используйте предкомпилированный wheel:
   ```bash
   pip install pipwin
   pipwin install pyaudio
   ```

### Модель Vosk не найдена

Убедитесь, что:
1. Модель скачана и распакована в папку `models/`
2. Путь в `config.json` правильный
3. Структура папки модели содержит подпапки `am/` и `graph/`

### Микрофон не работает

1. Проверьте, что микрофон подключен и работает
2. Убедитесь, что микрофон выбран как устройство по умолчанию в Windows
3. Проверьте права доступа приложения к микрофону

### Текст не вводится

1. Убедитесь, что активное окно принимает текстовый ввод
2. Проверьте логи в файле `voice_input.log`
3. Попробуйте запустить приложение от имени администратора

### Низкая точность распознавания

1. Используйте более точную модель Vosk (например, `vosk-model-ru-0.22`)
2. Улучшите качество микрофона
3. Уменьшите фоновый шум
4. Говорите четко и не слишком быстро

## Логирование

Приложение создает файл `voice_input.log` с подробной информацией о работе. Уровень логирования можно изменить в `config.json`:

```json
"log_level": "INFO"  // DEBUG, INFO, WARNING, ERROR
```

## Разработка

### Структура проекта

```
audio/
├── src/
│   ├── __init__.py
│   ├── main.py              # Главный модуль
│   ├── config.py            # Управление конфигурацией
│   ├── audio_capture.py     # Захват аудио
│   ├── speech_recognition.py # Распознавание речи
│   ├── text_input.py        # Ввод текста
│   ├── voice_commands.py    # Обработка команд
│   ├── system_tray.py       # Системный трей
│   └── hotkey_manager.py    # Горячие клавиши
├── models/                  # Модели Vosk
├── config.json              # Конфигурация
├── requirements.txt         # Зависимости
├── setup.py                 # Установщик
├── build_exe.spec          # Спецификация PyInstaller
└── README.md               # Документация
```

## Лицензия

MIT License

## Авторы

Voice Input Utility Development Team

## Благодарности

- [Vosk](https://alphacephei.com/vosk/) - за отличную библиотеку распознавания речи
- [PyAudio](https://people.csail.mit.edu/hubert/pyaudio/) - за работу с аудио
- Все разработчики используемых библиотек

