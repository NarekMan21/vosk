# 🤖 AI Agent Instructions — VoiceInput

> **ВАЖНО ДЛЯ AI:** Читай этот документ ПЕРВЫМ при каждой новой сессии.
> Он содержит всё необходимое для понимания проекта и продолжения работы.

---

## 🧠 ОБЯЗАТЕЛЬНЫЕ ПРАВИЛА ДЛЯ AI

### После КАЖДОГО изменения в коде:

1. **Обнови эту документацию** — добавь что изменилось
2. **Обнови версию** в `src/updater.py` → `CURRENT_VERSION`
3. **Обнови версию** в `installer.iss` → `MyAppVersion`
4. **Пересобери exe** → `python -m PyInstaller --clean build_exe.spec`
5. **Создай установщик** → `& "D:\Program Files (x86)\Inno Setup 6\ISCC.exe" installer.iss`
6. **Сделай коммит** → `git add -A && git commit -m "vX.X.X: Описание"`
7. **Запуши на GitHub** → `git push origin main`
8. **Создай релиз** → `gh release create vX.X.X "output/VoiceInput-Setup-X.X.X.exe" --title "VoiceInput vX.X.X" --notes-file release_notes.txt`

### Формат документирования изменений:

```markdown
### XX. Название функции

**Статус:** ✅ РЕАЛИЗОВАНО (vX.X.X)

**Что делает:** Краткое описание

**Файлы:**
- `src/file.py` — что изменено

**Как использовать:** Инструкция для пользователя
```

---

## 📋 META — Информация о проекте

```yaml
project: VoiceInput
description: Голосовой ввод текста для Windows с распознаванием речи Vosk
language: Python 3.10+
platform: Windows 10/11
repository: https://github.com/NarekMan21/vosk
current_version: "1.0.5"
last_update: "2026-01-17"

paths:
  workspace: d:\cursor\audio
  source: d:\cursor\audio\src
  config: d:\cursor\audio\config.json
  models: d:\cursor\audio\models
  output: d:\cursor\audio\output  # Установщики
  dist: d:\cursor\audio\dist      # Exe файлы
  docs: d:\cursor\audio\docs

key_files:
  main: src/main.py               # Главный файл приложения
  config: src/config.py           # Управление настройками
  audio: src/audio_capture.py     # Захват аудио с микрофона
  speech: src/speech_recognition.py  # Распознавание речи Vosk
  tray: src/system_tray.py        # Иконка в трее
  hotkeys: src/hotkey_manager.py  # Горячие клавиши
  updater: src/updater.py         # Проверка обновлений
  themes: src/themes.py           # Тёмная/светлая тема
  first_run: src/first_run.py     # Туториал первого запуска
  
build_files:
  spec: build_exe.spec            # PyInstaller спецификация
  installer: installer.iss        # Inno Setup скрипт
  
tools:
  inno_setup: "D:\Program Files (x86)\Inno Setup 6\ISCC.exe"
```

---

## 🏗️ АРХИТЕКТУРА ПРИЛОЖЕНИЯ

### Диаграмма компонентов

```
┌─────────────────────────────────────────────────────────────────┐
│                        VoiceInputApp                             │
│                         (main.py)                                │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────────────┐   │
│  │ AudioCapture │  │ SpeechRecog  │  │    TextInput         │   │
│  │ (микрофон)   │→ │ (Vosk)       │→ │ (вставка текста)     │   │
│  └──────────────┘  └──────────────┘  └──────────────────────┘   │
│         ↑                                                        │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────────────┐   │
│  │ HotkeyMgr    │  │ SystemTray   │  │    Config            │   │
│  │ (горячие кл) │  │ (иконка)     │  │ (настройки JSON)     │   │
│  └──────────────┘  └──────────────┘  └──────────────────────┘   │
│                                                                  │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────────────┐   │
│  │ Updater      │  │ Themes       │  │    Notifications     │   │
│  │ (обновления) │  │ (тёмная тема)│  │ (Toast уведомления)  │   │
│  └──────────────┘  └──────────────┘  └──────────────────────┘   │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

### Поток данных

```
1. Пользователь нажимает горячую клавишу (Ctrl+Shift+R)
2. HotkeyManager → вызывает toggle() в VoiceInputApp
3. AudioCapture → начинает захват аудио с микрофона
4. Аудио → отправляется в SpeechRecognition (Vosk)
5. Vosk → возвращает распознанный текст
6. TextInput → вставляет текст в активное окно
7. Повторяется пока не нажата клавиша снова
```

### Потоки (Threads)

```
Main Thread:
├── Tkinter UI (настройки, диалоги)
├── Pystray (иконка в трее)
└── Обработка событий

Background Threads:
├── AudioCapture._record_thread — непрерывный захват аудио
├── SpeechRecognition._process_thread — распознавание речи
├── Updater._check_thread — проверка обновлений
└── Notifications — показ Toast уведомлений
```

---

## 🔧 КЛЮЧЕВЫЕ КЛАССЫ И ФУНКЦИИ

### VoiceInputApp (main.py)

```python
class VoiceInputApp:
    """Главный класс приложения."""
    
    def __init__(self):
        # Инициализация всех компонентов
        self.config = Config()
        self.audio_capture = AudioCapture()
        self.speech_recognition = SpeechRecognition()
        self.text_input = TextInput()
        self.hotkey_manager = HotkeyManager()
        self.system_tray = SystemTray()
        
    def toggle(self):
        """Переключить распознавание вкл/выкл."""
        
    def start(self):
        """Начать распознавание."""
        
    def stop(self):
        """Остановить распознавание."""
        
    def open_settings(self):
        """Открыть окно настроек."""
        
    def shutdown(self):
        """Завершить приложение."""
```

### Config (config.py)

```python
class Config:
    """Управление настройками в config.json."""
    
    # Дефолтные значения
    DEFAULTS = {
        "hotkeys": {
            "toggle": "ctrl+shift+r",
            "hold_mode": False  # push-to-talk режим
        },
        "input_method": "keyboard",  # keyboard или clipboard
        "microphone_index": None,    # None = системный по умолчанию
        "model_path": "models/vosk-model-small-ru-0.22",
        "sample_rate": 16000,
        "chunk_size": 4000,
        "check_updates": True,
        "tutorial_shown": False,
        "dark_theme": True
    }
    
    def get(self, key, default=None): ...
    def set(self, key, value): ...
    def save(self): ...  # Атомарное сохранение
```

### AudioCapture (audio_capture.py)

```python
class AudioCapture:
    """Захват аудио с микрофона через PyAudio."""
    
    @staticmethod
    def list_devices() -> List[dict]:
        """Список доступных микрофонов."""
        
    @staticmethod
    def _fix_device_name(name: str) -> str:
        """Исправление кодировки названий устройств."""
        # Пробует: CP1251→UTF-8, Latin1→UTF-8, Latin1→CP1251, CP1252→CP1251
        
    def start(self): ...
    def stop(self): ...
    def reconnect(self): ...
```

### Updater (updater.py)

```python
CURRENT_VERSION = "1.0.5"  # ← ОБНОВЛЯТЬ ПРИ КАЖДОМ РЕЛИЗЕ!
GITHUB_REPO = "NarekMan21/vosk"

class UpdateChecker:
    """Проверка обновлений через GitHub Releases API."""
    
    def check_for_updates(self, on_result, silent=False): ...
    def show_update_dialog(self, version, url, notes, installer_url=None): ...
    
def download_and_install_update(download_url, on_progress=None):
    """Скачать и запустить установщик автоматически."""
```

---

## 📦 СБОРКА И РЕЛИЗ

### 1. Сборка EXE

```powershell
cd D:\cursor\audio
python -m PyInstaller --clean build_exe.spec
```

Результат: `dist/VoiceInput.exe`

### 2. Сборка установщика

```powershell
& "D:\Program Files (x86)\Inno Setup 6\ISCC.exe" installer.iss
```

Результат: `output/VoiceInput-Setup-X.X.X.exe`

### 3. Создание релиза на GitHub

```powershell
# 1. Коммит
git add -A
git commit -m "v1.0.5: Описание изменений"
git push origin main

# 2. Создать файл заметок
# (записать в release_notes.txt)

# 3. Создать релиз
gh release create v1.0.5 "output/VoiceInput-Setup-1.0.5.exe" --title "VoiceInput v1.0.5" --notes-file release_notes.txt

# 4. Удалить временный файл
Remove-Item release_notes.txt
```

### Важные файлы для сборки

| Файл | Описание |
|------|----------|
| `build_exe.spec` | PyInstaller конфигурация, **hiddenimports** для новых модулей |
| `installer.iss` | Inno Setup скрипт, **MyAppVersion** для версии |
| `src/updater.py` | **CURRENT_VERSION** — текущая версия |

### При добавлении нового модуля:

1. Создать файл в `src/`
2. Добавить в `hiddenimports` в `build_exe.spec`:
   ```python
   hiddenimports=[
       ...
       'new_module',  # ← добавить
   ]
   ```
3. Пересобрать exe

---

## ✅ РЕАЛИЗОВАННЫЕ ФУНКЦИИ

### Версия 1.0.5 (текущая)

#### 31. Тёмная тема UI

**Статус:** ✅ РЕАЛИЗОВАНО

**Что делает:** Тёмная цветовая схема для окна настроек

**Файлы:**
- `src/themes.py` — модуль с цветами тёмной/светлой темы
- `src/main.py` — применение темы к окну настроек
- `src/config.py` — настройка `dark_theme`

**Настройки:** Система → Тёмная тема (checkbox)

---

#### 32. Автоустановка обновлений

**Статус:** ✅ РЕАЛИЗОВАНО

**Что делает:** При обнаружении обновления можно нажать "Установить сейчас" — программа скачает и запустит установщик автоматически

**Файлы:**
- `src/updater.py` — функции `download_and_install_update()`, `get_installer_download_url()`

**Как работает:**
1. При обнаружении обновления показывается диалог
2. Кнопка "Установить сейчас" скачивает установщик с прогресс-баром
3. Запускается установщик с флагами `/SILENT /CLOSEAPPLICATIONS`
4. Старое приложение закрывается автоматически

---

### Версия 1.0.4

#### 28-30. Исправления кодировки и логирования

(см. IMPROVEMENTS.md для деталей)

---

### Версия 1.0.1-1.0.3

#### 23. Туториал первого запуска

**Статус:** ✅ РЕАЛИЗОВАНО

**Файлы:** `src/first_run.py`

**Настройка:** `tutorial_shown` в config.json

---

#### 24. Автопроверка обновлений

**Статус:** ✅ РЕАЛИЗОВАНО

**Файлы:** `src/updater.py`

**Настройки:** 
- `check_updates` — проверять при запуске
- Кнопка "Проверить обновления" в настройках

---

#### 25. Режим зажатия (Push-to-Talk)

**Статус:** ✅ РЕАЛИЗОВАНО

**Файлы:** `src/hotkey_manager.py`, `src/main.py`

**Настройка:** Горячие клавиши → Режим зажатия

---

#### 26. Исправление бага настроек

**Статус:** ✅ РЕАЛИЗОВАНО

**Проблема:** Окно настроек иногда не открывалось

**Решение:** `try...finally` для сброса флага `settings_window_open`

---

#### 27. Инсталлятор Inno Setup

**Статус:** ✅ РЕАЛИЗОВАНО

**Файлы:** `installer.iss`

---

## ⏳ ЗАПЛАНИРОВАННЫЕ УЛУЧШЕНИЯ

См. `IMPROVEMENTS.md` для полного списка. Приоритетные:

### Волна 1-2 (Стабильность)
- #1 Ротация логов
- #2 Single-instance (mutex)
- #5 Graceful shutdown
- #14 Валидация config.json
- #15 Атомарное сохранение конфига

### Волна 3-4 (UX + Производительность)
- #6 Звуковая обратная связь
- #9 Toast-уведомления
- #11 VAD фильтр тишины

---

## 🔍 ОТЛАДКА

### Логи

```powershell
# Просмотр логов
Get-Content D:\cursor\audio\voice_input.log -Tail 50

# Или в реальном времени
Get-Content D:\cursor\audio\voice_input.log -Wait
```

### Частые проблемы

| Проблема | Причина | Решение |
|----------|---------|---------|
| EXE не запускается | Модуль не в hiddenimports | Добавить в `build_exe.spec` |
| "Иероглифы" в микрофонах | Кодировка Windows | `_fix_device_name()` в `audio_capture.py` |
| Обновления не работают | Приватный репозиторий | Сделать публичным: `gh repo edit --visibility public --accept-visibility-change-consequences` |
| Настройки не открываются | Флаг застрял | Перезапустить приложение |

### Проверка API обновлений

```powershell
(Invoke-RestMethod -Uri "https://api.github.com/repos/NarekMan21/vosk/releases/latest").tag_name
```

---

## 📁 СТРУКТУРА ПРОЕКТА

```
d:\cursor\audio\
├── src/                    # Исходный код
│   ├── main.py            # Главный файл
│   ├── config.py          # Настройки
│   ├── audio_capture.py   # Микрофон
│   ├── speech_recognition.py  # Vosk
│   ├── text_input.py      # Вставка текста
│   ├── system_tray.py     # Иконка в трее
│   ├── hotkey_manager.py  # Горячие клавиши
│   ├── updater.py         # Обновления
│   ├── themes.py          # Тёмная тема
│   ├── first_run.py       # Туториал
│   ├── notifications.py   # Toast уведомления
│   ├── audio_feedback.py  # Звуки
│   ├── autostart.py       # Автозапуск
│   └── vad.py             # Voice Activity Detection
│
├── models/                 # Модели Vosk
│   ├── vosk-model-small-ru-0.22/  # Малая модель (45 МБ)
│   └── vosk-model-ru-0.42/        # Большая модель (1.8 ГБ)
│
├── docs/                   # Документация
│   ├── AI_INSTRUCTIONS.md # ← ТЫ ЗДЕСЬ
│   ├── IMPROVEMENTS.md    # Все улучшения
│   ├── IMPLEMENTATION_GUIDE.md  # Чек-листы
│   ├── QUICK_REFERENCE.md # Краткая памятка
│   └── README.md          # Описание docs
│
├── dist/                   # Собранный exe
│   ├── VoiceInput.exe
│   └── config.json
│
├── output/                 # Установщики
│   └── VoiceInput-Setup-X.X.X.exe
│
├── build_exe.spec          # PyInstaller конфиг
├── installer.iss           # Inno Setup скрипт
├── config.json             # Настройки (dev)
├── requirements.txt        # Зависимости Python
└── voice_input.log         # Логи
```

---

## ❓ FAQ ДЛЯ AI

### Как добавить новую функцию?

1. Реализовать код в `src/`
2. Если новый файл — добавить в `hiddenimports` в `build_exe.spec`
3. Обновить версию в `src/updater.py` и `installer.iss`
4. Пересобрать exe и установщик
5. **ОБНОВИТЬ ЭТУ ДОКУМЕНТАЦИЮ!**
6. Коммит, пуш, релиз

### Как исправить баг?

1. Найти проблему в логах или коде
2. Исправить
3. Протестировать
4. Обновить версию (инкремент patch: 1.0.5 → 1.0.6)
5. Пересобрать и выпустить
6. Документировать исправление

### Что делать если exe не запускается?

1. Проверить `hiddenimports` в `build_exe.spec`
2. Проверить что все новые модули добавлены
3. Пересобрать с `--clean`
4. Проверить логи (если создаются)

### Как проверить работу обновлений?

```powershell
# Проверить что репозиторий публичный
gh repo view NarekMan21/vosk --json visibility

# Проверить последний релиз
(Invoke-RestMethod -Uri "https://api.github.com/repos/NarekMan21/vosk/releases/latest").tag_name
```

---

## 📝 ШАБЛОН ДЛЯ ДОКУМЕНТИРОВАНИЯ НОВОГО УЛУЧШЕНИЯ

```markdown
### XX. Название

**Статус:** ✅ РЕАЛИЗОВАНО (vX.X.X)

**Цель:** Зачем это нужно

**Проблема:** Что было плохо до этого

**Решение:** Как решили

**Файлы:**
- `src/file.py` — описание изменений

**Настройки:** Где найти в UI

**Критерии готовности:**
- [x] Пункт 1
- [x] Пункт 2
```

---

## 🚨 КРИТИЧЕСКИ ВАЖНО

1. **ВСЕГДА обновляй документацию** после изменений
2. **ВСЕГДА обновляй версию** в двух местах: `updater.py` и `installer.iss`
3. **ВСЕГДА добавляй новые модули** в `hiddenimports`
4. **НИКОГДА не пуши exe в репозиторий** — только через Releases
5. **ТЕСТИРУЙ exe** перед релизом

---

*Последнее обновление: 17 января 2026, версия 1.0.5*
