# 🤖 AI Agent Master Document — VoiceInput

> **ВАЖНО ДЛЯ AI:** Этот документ — твоя главная точка входа в проект.
> Читай его ПЕРВЫМ при каждой новой сессии.

---

## 🧠 ЧТО ЭТО ЗА ПРОЕКТ

**VoiceInput** — Windows-приложение для голосового ввода текста с использованием офлайн распознавания речи (Vosk).

### Ключевые особенности:
- 🎤 **Офлайн распознавание** — работает без интернета (Vosk)
- ⌨️ **Горячие клавиши** — Ctrl+Shift+V для вкл/выкл
- 📋 **Автоматическая вставка** — текст вставляется в любое приложение
- 🔔 **Системный трей** — работает в фоне
- 🔄 **Автообновление** — проверка новых версий на GitHub
- 🌙 **Тёмная тема** — современный UI

### Технический стек:
```yaml
Язык: Python 3.10+
Платформа: Windows 10/11
Распознавание: Vosk (офлайн)
UI: Tkinter + pystray (системный трей)
Сборка: PyInstaller → Inno Setup
Репозиторий: https://github.com/NarekMan21/vosk
```

---

## 📁 СТРУКТУРА ПРОЕКТА

```
d:\cursor\audio\
├── src/                    # Исходный код
│   ├── main.py             # Главный файл, точка входа
│   ├── config.py           # Управление конфигурацией
│   ├── audio_capture.py    # Захват аудио с микрофона
│   ├── speech_recognition.py # Распознавание речи (Vosk)
│   ├── text_input.py       # Вставка текста (pyautogui/clipboard)
│   ├── hotkey_manager.py   # Горячие клавиши
│   ├── system_tray.py      # Иконка в трее
│   ├── notifications.py    # Windows Toast уведомления
│   ├── audio_feedback.py   # Звуковые сигналы
│   ├── autostart.py        # Автозапуск Windows
│   ├── first_run.py        # Туториал первого запуска
│   ├── updater.py          # Проверка обновлений GitHub
│   ├── themes.py           # Тёмная/светлая тема UI
│   ├── vad.py              # Voice Activity Detection
│   ├── voice_commands.py   # Голосовые команды
│   ├── model_manager.py    # Управление моделями
│   └── app_statistics.py   # Статистика использования
│
├── models/                 # Модели Vosk
│   ├── vosk-model-small-ru-0.22/   # Маленькая (45 МБ)
│   └── vosk-model-ru-0.42/         # Большая (1.8 ГБ)
│
├── docs/                   # Документация
│   ├── AI_INSTRUCTIONS.md  # ← ТЫ ЗДЕСЬ
│   ├── IMPROVEMENTS.md     # Описание улучшений
│   ├── IMPLEMENTATION_GUIDE.md # Чек-листы реализации
│   ├── QUICK_REFERENCE.md  # Краткая памятка
│   ├── UI_REDESIGN.md      # 🎨 План UI редизайна
│   └── README.md           # Индекс документации
│
├── dist/                   # Собранный exe
├── output/                 # Инсталляторы
├── config.json             # Конфигурация пользователя
├── build_exe.spec          # PyInstaller спецификация
├── installer.iss           # Inno Setup скрипт
├── requirements.txt        # Python зависимости
└── voice_input.log         # Лог-файл
```

---

## 🎯 ТЕКУЩЕЕ СОСТОЯНИЕ

```yaml
Версия: 1.0.5
Дата: 17 января 2026
Репозиторий: ПУБЛИЧНЫЙ (автообновление работает)
Последний релиз: https://github.com/NarekMan21/vosk/releases/tag/v1.0.5
```

### Реализованные функции (v1.0.5):

| # | Функция | Файл | Статус |
|---|---------|------|--------|
| 23 | Туториал первого запуска | `first_run.py` | ✅ |
| 24 | Автопроверка обновлений | `updater.py` | ✅ |
| 25 | Режим зажатия (push-to-talk) | `hotkey_manager.py` | ✅ |
| 26 | Исправление бага настроек | `main.py` | ✅ |
| 27 | Инсталлятор Inno Setup | `installer.iss` | ✅ |
| 28 | Кодировка микрофонов | `audio_capture.py` | ✅ |
| 29 | Кнопка проверки обновлений | `main.py` | ✅ |
| 30 | Улучшенные логи | `updater.py` | ✅ |
| 31 | Тёмная тема | `themes.py` | ✅ |
| 32 | Автоустановка обновлений | `updater.py` | ✅ |

### Запланировано (UI Редизайн):

> **См. полный план:** [UI_REDESIGN.md](UI_REDESIGN.md)

| # | Функция | Статус |
|---|---------|--------|
| UI-1 | Новая цветовая палитра | ⏳ |
| UI-6 | CustomTkinter виджеты | ⏳ |
| UI-10 | Toggle Switch вместо чекбоксов | ⏳ |

---

## ⚠️ КРИТИЧЕСКИЕ ПРАВИЛА ДЛЯ AI

### 🔴 ПРАВИЛО 1: ОБНОВЛЕНИЕ GITHUB

> **После КАЖДОГО успешного изменения — СПРОСИ пользователя:**
> "Хочешь опубликовать изменения на GitHub для обновления у пользователей?"

**Workflow публикации (только после подтверждения пользователя):**

```bash
# 1. Обновить версию в updater.py
CURRENT_VERSION = "X.X.X"

# 2. Обновить версию в installer.iss
#define MyAppVersion "X.X.X"

# 3. Собрать exe
python -m PyInstaller --clean build_exe.spec

# 4. Создать инсталлятор
& "D:\Program Files (x86)\Inno Setup 6\ISCC.exe" installer.iss

# 5. Коммит и пуш
git add -A
git commit -m "vX.X.X: Описание изменений"
git push origin main

# 6. Создать релиз на GitHub
gh release create vX.X.X "output/VoiceInput-Setup-X.X.X.exe" --title "VoiceInput vX.X.X" --notes-file release_notes.txt
```

### 🔴 ПРАВИЛО 2: ОБНОВЛЕНИЕ ДОКУМЕНТАЦИИ

> **После КАЖДОГО изменения — обнови документацию!**

Что обновить:
1. `IMPROVEMENTS.md` — добавить новое улучшение
2. `IMPLEMENTATION_GUIDE.md` — обновить прогресс
3. `QUICK_REFERENCE.md` — обновить таблицу статусов
4. `AI_INSTRUCTIONS.md` — обновить "Текущее состояние"

### 🔴 ПРАВИЛО 3: ТЕСТИРОВАНИЕ

> **Перед публикацией — проверь что exe работает!**

```bash
# Запустить собранный exe
.\dist\VoiceInput.exe

# Проверить:
# - Приложение запускается
# - Иконка в трее появляется
# - Горячие клавиши работают
# - Распознавание работает
```

---

## 🔧 ЧАСТЫЕ ЗАДАЧИ

### Добавить новую функцию

1. Реализовать в соответствующем файле `src/`
2. Добавить настройку в `config.py` если нужно
3. Добавить UI в `main.py` если нужно
4. Добавить в `hiddenimports` в `build_exe.spec` если новый модуль
5. Протестировать локально: `python src/main.py`
6. Обновить документацию
7. **СПРОСИТЬ** пользователя о публикации на GitHub

### Исправить баг

1. Найти причину в логах: `voice_input.log`
2. Исправить код
3. Протестировать
4. Обновить версию (patch: X.X.+1)
5. Обновить документацию
6. **СПРОСИТЬ** пользователя о публикации на GitHub

### Собрать новую версию

```bash
# 1. Обновить версию
# src/updater.py: CURRENT_VERSION = "X.X.X"
# installer.iss: #define MyAppVersion "X.X.X"

# 2. Собрать exe
python -m PyInstaller --clean build_exe.spec

# 3. Создать инсталлятор
& "D:\Program Files (x86)\Inno Setup 6\ISCC.exe" installer.iss

# 4. Результат в:
# - dist/VoiceInput.exe
# - output/VoiceInput-Setup-X.X.X.exe
```

---

## 📋 КОНФИГУРАЦИЯ

### Файл: `config.json`

```json
{
    "audio": {
        "sample_rate": 16000,
        "chunk_size": 4000,
        "device_index": null
    },
    "model": {
        "path": "models/vosk-model-small-ru-0.22",
        "name": "vosk-model-small-ru-0.22"
    },
    "hotkeys": {
        "toggle": "ctrl+shift+v",
        "hold_mode": false
    },
    "input_method": "clipboard",
    "beep_enabled": true,
    "notifications_enabled": true,
    "autostart": false,
    "log_level": "INFO",
    "tutorial_shown": true,
    "check_updates": true,
    "dark_theme": true
}
```

### Ключевые настройки:

| Ключ | Тип | Описание |
|------|-----|----------|
| `hotkeys.toggle` | string | Горячая клавиша вкл/выкл |
| `hotkeys.hold_mode` | bool | Режим зажатия (push-to-talk) |
| `audio.device_index` | int/null | Индекс микрофона (null = по умолчанию) |
| `input_method` | string | "clipboard" или "keyboard" |
| `check_updates` | bool | Проверять обновления при запуске |
| `dark_theme` | bool | Тёмная тема в настройках |

---

## 🐛 ИЗВЕСТНЫЕ ПРОБЛЕМЫ И РЕШЕНИЯ

### Проблема: "Иероглифы" в названиях микрофонов

**Причина:** PyAudio на Windows неправильно декодирует CP1251.

**Решение:** Метод `_fix_device_name()` в `audio_capture.py` пробует разные стратегии перекодирования.

**Статус:** Частично решено, может требовать доработки на некоторых системах.

---

### Проблема: Автообновление не работает

**Возможные причины:**
1. Репозиторий приватный → сделать публичным
2. Нет релизов → создать релиз через `gh release create`
3. Нет интернета → проверить сеть

**Диагностика:** Смотреть `voice_input.log` — там есть логи проверки обновлений.

---

### Проблема: Настройки не открываются

**Причина:** Флаг `settings_window_open` застрял в `True`.

**Решение:** Реализовано в v1.0.1 — `try...finally` для гарантированного сброса флага.

---

## 🔗 ВАЖНЫЕ ССЫЛКИ

- **Репозиторий:** https://github.com/NarekMan21/vosk
- **Релизы:** https://github.com/NarekMan21/vosk/releases
- **API релизов:** https://api.github.com/repos/NarekMan21/vosk/releases/latest
- **Vosk модели:** https://alphacephei.com/vosk/models

---

## 📝 ЧЕК-ЛИСТ ДЛЯ КАЖДОЙ СЕССИИ

Перед началом работы:
- [ ] Прочитал этот документ
- [ ] Понял текущую версию и состояние
- [ ] Понял что нужно сделать

После внесения изменений:
- [ ] Код работает (проверил запуском)
- [ ] Нет ошибок в логах
- [ ] Обновил документацию
- [ ] **СПРОСИЛ пользователя о публикации на GitHub**

После подтверждения публикации:
- [ ] Обновил версию
- [ ] Собрал exe
- [ ] Создал инсталлятор
- [ ] Закоммитил и запушил
- [ ] Создал релиз на GitHub
- [ ] Обновил документацию (финальная версия)

---

## 🚀 БЫСТРЫЙ СТАРТ ДЛЯ AI

**Если пользователь просит что-то сделать:**

1. **Понять задачу** — что именно нужно?
2. **Найти файлы** — где это реализовано/нужно реализовать?
3. **Внести изменения** — написать код
4. **Протестировать** — `python src/main.py`
5. **Обновить документацию** — добавить в IMPROVEMENTS.md
6. **СПРОСИТЬ** — "Хочешь опубликовать на GitHub?"
7. **Публикация** — только после подтверждения пользователя

---

*Последнее обновление: 17 января 2026, v1.0.5*
