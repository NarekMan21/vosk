# 📚 Документация улучшений VoiceInput

## Оглавление

1. [Критичные улучшения](#-критичные-улучшения)
2. [UX улучшения](#-ux-улучшения)
3. [VAD и производительность](#-vad-и-производительность)
4. [Конфигурация и настройки](#-конфигурация-и-настройки)
5. [Статистика и логирование](#-статистика-и-логирование)

---

## 🔴 Критичные улучшения

### 1. Ротация логов

**Цель:** Предотвратить неограниченный рост лог-файла.

**Проблема:** Текущий лог `voice_input.log` растёт бесконечно, что может привести к заполнению диска.

**Решение:** 
- Использовать `RotatingFileHandler` вместо обычного `FileHandler`
- Максимальный размер файла: 5 МБ
- Хранить 3 резервные копии (voice_input.log.1, .2, .3)

**Затрагиваемые файлы:**
- `src/main.py` — настройка логирования

**Зависимости:** Нет

**Критерии готовности:**
- [ ] Лог-файл не превышает 5 МБ
- [ ] При достижении лимита создаётся backup
- [ ] Старые backup-файлы автоматически удаляются
- [ ] Приложение запускается без ошибок
- [ ] Логирование работает корректно

---

### 2. Single-instance (Mutex)

**Цель:** Запретить запуск нескольких экземпляров приложения одновременно.

**Проблема:** Два экземпляра конфликтуют за микрофон и горячие клавиши.

**Решение:**
- Создать именованный mutex при старте
- Если mutex уже существует — показать уведомление и выйти
- Опционально: активировать существующее окно

**Затрагиваемые файлы:**
- `src/main.py` — проверка при запуске

**Зависимости:** Нет

**Критерии готовности:**
- [ ] При запуске второго экземпляра появляется уведомление
- [ ] Второй экземпляр корректно завершается
- [ ] Первый экземпляр продолжает работать
- [ ] Mutex освобождается при завершении приложения
- [ ] Работает после аварийного завершения предыдущего экземпляра

---

### 3. Обработка ошибок микрофона

**Цель:** Информировать пользователя о проблемах с микрофоном.

**Проблема:** При отключении микрофона приложение падает без уведомления.

**Решение:**
- Перехватывать исключения PyAudio
- Показывать Toast-уведомление при ошибке
- Обновлять иконку трея (красный = ошибка)
- Логировать детали ошибки

**Затрагиваемые файлы:**
- `src/audio_capture.py` — обработка исключений
- `src/main.py` — callback при ошибке

**Зависимости:** 
- #9 Toast-уведомления (опционально)

**Критерии готовности:**
- [ ] При отключении микрофона появляется уведомление
- [ ] Приложение НЕ падает
- [ ] Иконка трея меняется на "ошибка"
- [ ] В логе есть информация об ошибке
- [ ] После подключения микрофона можно возобновить работу

---

### 4. Reconnect при потере микрофона

**Цель:** Автоматически восстанавливать работу после сбоя микрофона.

**Проблема:** После ошибки требуется ручной перезапуск.

**Решение:**
- Периодически проверять доступность устройства
- При восстановлении — автоматически переподключаться
- Использовать exponential backoff (1с, 2с, 4с...)
- Максимум 5 попыток, затем ждать ручного действия

**Затрагиваемые файлы:**
- `src/audio_capture.py` — логика reconnect

**Зависимости:**
- #3 Обработка ошибок микрофона

**Критерии готовности:**
- [ ] После кратковременного отключения микрофон переподключается
- [ ] Показывается уведомление "Переподключение..."
- [ ] После успешного reconnect показывается "Микрофон подключён"
- [ ] При невозможности reconnect — понятное сообщение
- [ ] Интервалы между попытками увеличиваются

---

### 5. Graceful Shutdown

**Цель:** Корректно освобождать все ресурсы при завершении.

**Проблема:** При закрытии через диспетчер задач могут остаться зависшие потоки.

**Решение:**
- Регистрировать обработчики сигналов (SIGTERM, SIGINT)
- Использовать `atexit` для cleanup
- Таймаут на ожидание потоков (5 секунд)
- Принудительное завершение если таймаут истёк

**Затрагиваемые файлы:**
- `src/main.py` — обработчики сигналов и cleanup

**Зависимости:** Нет

**Критерии готовности:**
- [ ] Ctrl+C в консоли корректно завершает приложение
- [ ] "Выход" из трея корректно завершает приложение
- [ ] PyAudio stream закрывается без ошибок
- [ ] Все потоки завершаются
- [ ] Лог-файл содержит запись о завершении
- [ ] Mutex (#2) освобождается

---

## 🟡 UX улучшения

### 6. Звуковая обратная связь

**Цель:** Дать пользователю аудио-подтверждение действий.

**Проблема:** Пользователь не знает, включилось ли распознавание, если не смотрит на трей.

**Решение:**
- Короткий звук при включении (высокий тон, 100мс)
- Короткий звук при выключении (низкий тон, 100мс)
- Опциональный звук при ошибке
- Настройка: включить/выключить звуки

**Затрагиваемые файлы:**
- Новый: `src/audio_feedback.py`
- `src/main.py` — вызов звуков
- `src/config.py` — настройка

**Зависимости:** Нет

**Критерии готовности:**
- [ ] При нажатии горячей клавиши воспроизводится звук
- [ ] Звук отличается для вкл/выкл
- [ ] Звук НЕ блокирует работу (асинхронный)
- [ ] Можно отключить в настройках
- [ ] Звук слышен даже если фокус в другом приложении

---

### 7. Индикатор загрузки модели

**Цель:** Показать пользователю прогресс загрузки модели.

**Проблема:** При загрузке большой модели (1-2 ГБ) пользователь думает, что приложение зависло.

**Решение:**
- Анимированная иконка трея во время загрузки
- Tooltip: "Загрузка модели..."
- Toast-уведомление при завершении загрузки
- Опционально: прогресс-бар в уведомлении

**Затрагиваемые файлы:**
- `src/system_tray.py` — анимация и tooltip
- `src/main.py` — события загрузки

**Зависимости:**
- #9 Toast-уведомления (желательно)

**Критерии готовности:**
- [ ] Во время загрузки иконка анимируется
- [ ] Tooltip показывает "Загрузка модели..."
- [ ] После загрузки — уведомление "Готово к работе"
- [ ] При ошибке загрузки — уведомление с причиной
- [ ] Анимация не потребляет много CPU

---

### 8. Выбор микрофона

**Цель:** Позволить пользователю выбрать конкретный микрофон.

**Проблема:** Используется только устройство по умолчанию, что неудобно при нескольких микрофонах.

**Решение:**
- Список устройств в окне настроек
- Кнопка "Тест" для проверки микрофона
- Сохранение выбора в config.json
- Fallback на default если выбранный недоступен

**Затрагиваемые файлы:**
- `src/audio_capture.py` — список устройств, выбор
- `src/main.py` — UI настроек
- `src/config.py` — сохранение выбора

**Зависимости:**
- #3 Обработка ошибок (для fallback)

**Критерии готовности:**
- [ ] В настройках виден список микрофонов
- [ ] Можно выбрать любой микрофон
- [ ] Кнопка "Тест" показывает уровень сигнала
- [ ] Выбор сохраняется между сессиями
- [ ] При недоступности микрофона — fallback + уведомление

---

### 9. Toast-уведомления Windows

**Цель:** Показывать системные уведомления Windows.

**Проблема:** Пользователь не видит важные события если окно свёрнуто.

**Решение:**
- Использовать библиотеку `win11toast` или `windows-toasts`
- Уведомления: старт, стоп, ошибка, загрузка завершена
- Настройка: включить/выключить

**Затрагиваемые файлы:**
- Новый: `src/notifications.py`
- `src/main.py` — вызов уведомлений
- `src/config.py` — настройка
- `requirements.txt` — новая зависимость

**Зависимости:** Нет

**Критерии готовности:**
- [ ] При включении распознавания появляется уведомление
- [ ] Уведомление содержит иконку приложения
- [ ] Уведомление исчезает через 3-5 секунд
- [ ] Можно отключить в настройках
- [ ] Работает на Windows 10 и 11

---

### 10. Анимация иконки трея

**Цель:** Визуально показать, что идёт распознавание речи.

**Проблема:** Статичная иконка не показывает активность.

**Решение:**
- Пульсация иконки когда обнаружена речь
- 4-8 кадров анимации
- Частота: 5-10 FPS
- Возврат к статичной иконке при тишине

**Затрагиваемые файлы:**
- `src/system_tray.py` — генерация кадров, таймер

**Зависимости:**
- #11 VAD (для определения речи vs тишина)

**Критерии готовности:**
- [ ] Иконка пульсирует при обнаружении речи
- [ ] Анимация плавная
- [ ] Анимация останавливается при тишине
- [ ] CPU-usage анимации < 1%
- [ ] Работает при многочасовой работе

---

## 🟢 VAD и производительность

### 11. VAD фильтр тишины

**Цель:** Не обрабатывать тишину, экономить CPU.

**Проблема:** Vosk обрабатывает даже тишину, нагружая CPU.

**Решение:**
- Использовать `webrtcvad` для определения речи
- Пропускать чанки без речи
- Настраиваемая агрессивность (0-3)
- Гистерезис для избежания "дёргания"

**Затрагиваемые файлы:**
- Новый: `src/vad.py`
- `src/main.py` — интеграция VAD
- `src/config.py` — настройки VAD
- `requirements.txt` — webrtcvad

**Зависимости:** Нет

**Критерии готовности:**
- [ ] CPU-usage в тишине < 5%
- [ ] Речь распознаётся корректно
- [ ] Нет пропуска начала фраз
- [ ] Настройка агрессивности работает
- [ ] VAD можно отключить

---

### 12. Увеличение chunk_size

**Цель:** Уменьшить накладные расходы на обработку.

**Проблема:** Маленький chunk_size = много переключений контекста.

**Решение:**
- Увеличить с 4000 до 8000 samples
- Это ~500мс буфера при 16kHz
- Баланс между задержкой и эффективностью

**Затрагиваемые файлы:**
- `src/config.py` — значение по умолчанию
- `config.json` — текущая конфигурация

**Зависимости:** Нет

**Критерии готовности:**
- [ ] chunk_size = 8000 в конфиге
- [ ] Распознавание работает корректно
- [ ] Задержка приемлемая (< 1 сек)
- [ ] CPU-usage снизился

---

### 13. Оптимизация параметров модели

**Цель:** Документировать настройки модели для разных сценариев.

**Проблема:** Пользователи не знают как оптимизировать.

**Решение:**
- Создать документацию по параметрам model.conf
- Примеры для: быстрый компьютер, слабый компьютер
- Объяснение влияния beam, max-active

**Затрагиваемые файлы:**
- Новый: `docs/MODEL_OPTIMIZATION.md`

**Зависимости:** Нет

**Критерии готовности:**
- [ ] Документация создана
- [ ] Примеры конфигураций понятны
- [ ] Описано влияние каждого параметра

---

## 🔵 Конфигурация и настройки

### 14. Валидация config.json

**Цель:** Защититься от битых или неполных конфигов.

**Проблема:** Ручное редактирование может сломать конфиг.

**Решение:**
- JSON Schema для валидации
- Fallback на дефолтные значения
- Логирование ошибок валидации
- Сообщение пользователю о проблемах

**Затрагиваемые файлы:**
- `src/config.py` — валидация при загрузке

**Зависимости:** Нет

**Критерии готовности:**
- [ ] Битый JSON не ломает приложение
- [ ] Недостающие поля заполняются defaults
- [ ] Некорректные значения заменяются defaults
- [ ] В логе есть информация об исправлениях
- [ ] Пользователь видит уведомление о проблемах

---

### 15. Атомарное сохранение конфига

**Цель:** Предотвратить потерю конфига при сбое.

**Проблема:** Crash во время записи = битый файл.

**Решение:**
- Записывать во временный файл
- Затем atomic rename
- Если rename не удался — оставить старый

**Затрагиваемые файлы:**
- `src/config.py` — метод save()

**Зависимости:** Нет

**Критерии готовности:**
- [ ] Сохранение использует temp файл
- [ ] При успехе — rename
- [ ] При ошибке — старый файл сохранён
- [ ] Нет временных файлов после сохранения

---

### 16. Расширенное окно настроек

**Цель:** Дать доступ ко всем настройкам через UI.

**Проблема:** Сейчас только горячие клавиши и метод ввода.

**Решение:**
- Вкладки: Общие, Аудио, Распознавание, Интерфейс
- Общие: автозапуск, язык
- Аудио: микрофон, VAD, chunk_size
- Распознавание: модель, параметры
- Интерфейс: звуки, уведомления

**Затрагиваемые файлы:**
- `src/main.py` — окно настроек

**Зависимости:**
- Многие другие улучшения для наполнения

**Критерии готовности:**
- [ ] Окно имеет вкладки
- [ ] Все настройки доступны
- [ ] Изменения применяются сразу или после "Сохранить"
- [ ] Есть кнопка "Сбросить по умолчанию"
- [ ] UI понятный и не перегруженный

---

### 17. Автозапуск при старте Windows

**Цель:** Запускать приложение автоматически.

**Проблема:** Нужно вручную запускать после каждой перезагрузки.

**Решение:**
- Checkbox в настройках
- Добавление/удаление из реестра автозагрузки
- Путь: HKCU\Software\Microsoft\Windows\CurrentVersion\Run

**Затрагиваемые файлы:**
- Новый: `src/autostart.py`
- `src/main.py` — настройки

**Зависимости:** Нет

**Критерии готовности:**
- [ ] Checkbox в настройках работает
- [ ] При включении — приложение стартует с Windows
- [ ] При выключении — удаляется из автозагрузки
- [ ] Работает для portable и installed версий
- [ ] Не требует прав администратора

---

## 🟣 Статистика и логирование

### 18. Статистика сессии

**Цель:** Показать пользователю статистику использования.

**Проблема:** Нет обратной связи о продуктивности.

**Решение:**
- Считать слова, фразы, время работы
- Хранить за сессию и за всё время
- Показывать в окне статистики

**Затрагиваемые файлы:**
- Новый: `src/statistics.py`
- `src/main.py` — сбор данных, UI

**Зависимости:** Нет

**Критерии готовности:**
- [ ] Счётчик слов за сессию
- [ ] Счётчик слов за всё время
- [ ] Время активной работы
- [ ] Данные сохраняются между сессиями
- [ ] Кнопка "Сбросить статистику"

---

### 19. Улучшенный tooltip в трее

**Цель:** Показать полезную информацию при наведении на иконку.

**Проблема:** Сейчас только статус.

**Решение:**
- Статус: Активен/Неактивен/Загрузка
- Микрофон: название устройства
- Сессия: слов распознано
- Модель: название

**Затрагиваемые файлы:**
- `src/system_tray.py` — формирование tooltip

**Зависимости:**
- #18 Статистика (для счётчика слов)

**Критерии готовности:**
- [ ] Tooltip многострочный
- [ ] Показывает актуальную информацию
- [ ] Обновляется в реальном времени
- [ ] Не обрезается Windows

---

---

## 🚀 Дополнительные улучшения

### 20. Скачивание моделей через UI

**Цель:** Упростить установку и смену моделей для пользователя.

**Проблема:** Пользователь должен вручную скачивать модели, знать куда их класть.

**Решение:**
- Список доступных моделей в настройках (small, medium, large)
- Кнопка "Скачать" с прогресс-баром
- Автоматическое размещение в правильную папку
- Возможность удалить неиспользуемые модели
- Показ размера и описания каждой модели

**UI-дизайн:**
```
┌─────────────────────────────────────────────────────┐
│  Модели распознавания                               │
├─────────────────────────────────────────────────────┤
│  ● vosk-model-small-ru-0.22     [45 МБ]  ✓ Активна │
│    Быстрая, базовое качество                        │
│    [Удалить]                                        │
│                                                     │
│  ○ vosk-model-ru-0.42           [1.8 ГБ] ✓ Скачана │
│    Высокое качество, требует больше RAM             │
│    [Сделать активной] [Удалить]                     │
│                                                     │
│  ○ vosk-model-ru-0.10           [2.5 ГБ] Не скачана│
│    Максимальное качество                            │
│    [Скачать ▼]                                      │
│                                                     │
│  ════════════════════════════════════════           │
│  Скачивание: vosk-model-ru-0.10                     │
│  [████████████░░░░░░░░] 62% (1.5 ГБ / 2.5 ГБ)       │
│  Скорость: 15 МБ/с | Осталось: ~1 мин              │
│  [Отмена]                                           │
└─────────────────────────────────────────────────────┘
```

**Затрагиваемые файлы:**
- Новый: `src/model_manager.py` — загрузка, удаление, список
- `src/main.py` — UI скачивания
- `src/config.py` — активная модель

**Зависимости:**
- #16 Расширенное окно настроек

**Критерии готовности:**
- [ ] Список моделей отображается корректно
- [ ] Скачивание с прогресс-баром работает
- [ ] Можно отменить скачивание
- [ ] Можно переключить активную модель
- [ ] Можно удалить неиспользуемую модель
- [ ] При ошибке сети — понятное сообщение
- [ ] Скачанные файлы проверяются (hash/размер)

---

### 21. Гибридная загрузка моделей

**Цель:** Быстрый старт приложения даже с большой моделью.

**Проблема:** Большая модель (1-2 ГБ) загружается 10-30 секунд, пользователь думает что зависло.

**Решение:**
- При старте загружать маленькую модель (если есть)
- Большую модель загружать в фоне
- После загрузки большой — автоматически переключиться
- Показывать статус загрузки в трее

**Алгоритм:**
```
1. Старт приложения
2. Есть маленькая модель? 
   → ДА: загрузить её (быстро, ~2 сек)
   → НЕТ: показать "Загрузка модели..."
3. Запустить фоновый поток загрузки большой модели
4. Показать в трее: "Готово (базовое качество)"
5. Когда большая загружена:
   → Переключиться на неё
   → Показать: "Переключено на высокое качество"
```

**UI индикация:**
```
Трей tooltip:
├── "Загрузка модели..." (при старте)
├── "Готово (быстрая модель)" (маленькая загружена)
├── "Загрузка улучшенной модели: 45%" (фон)
└── "Готово (высокое качество)" (большая загружена)
```

**Затрагиваемые файлы:**
- `src/main.py` — логика гибридной загрузки
- `src/speech_recognition.py` — переключение модели
- `src/system_tray.py` — индикация статуса

**Зависимости:**
- #7 Индикатор загрузки модели
- #20 Скачивание моделей (для наличия нескольких моделей)

**Критерии готовности:**
- [ ] Маленькая модель загружается первой
- [ ] Приложение готово к работе за < 5 сек
- [ ] Большая модель загружается в фоне
- [ ] Переключение происходит автоматически
- [ ] Пользователь видит статус в трее
- [ ] Нет прерывания распознавания при переключении

---

### 22. Простой и понятный UI настроек

**Цель:** Настройки не должны пугать обычного пользователя.

**Проблема:** Много технических опций могут запутать.

**Принципы дизайна:**
1. **Прогрессивное раскрытие** — базовые опции видны, продвинутые скрыты
2. **Понятные названия** — "Качество распознавания" вместо "beam width"
3. **Визуальные подсказки** — иконки, tooltips
4. **Предустановки** — "Быстро", "Сбалансировано", "Качественно"

**Макет главного окна настроек:**
```
┌─────────────────────────────────────────────────────┐
│  ⚙️ Настройки VoiceInput                      [X]  │
├─────────────────────────────────────────────────────┤
│                                                     │
│  🎤 Микрофон                                        │
│  ┌─────────────────────────────────┐                │
│  │ Realtek Microphone         ▼    │ [🔊 Тест]     │
│  └─────────────────────────────────┘                │
│                                                     │
│  🧠 Качество распознавания                          │
│  ○ Быстрое      ○ Сбалансированное   ● Точное      │
│  └─ CPU: низкий   └─ CPU: средний      └─ CPU: выше │
│                                                     │
│  🔊 Звуки                                           │
│  [✓] Звук при включении/выключении                 │
│                                                     │
│  📢 Уведомления                                     │
│  [✓] Показывать уведомления Windows                │
│                                                     │
│  ⌨️ Горячие клавиши                                 │
│  Включить/выключить: [Ctrl+Shift+V]                │
│  Пауза:              [Ctrl+Shift+P]                │
│                                                     │
│  ─────────────────────────────────────────────────  │
│  [▶ Дополнительные настройки...]                   │
│                                                     │
│          [Сохранить]  [Отмена]  [Сбросить]         │
└─────────────────────────────────────────────────────┘
```

**Макет дополнительных настроек:**
```
┌─────────────────────────────────────────────────────┐
│  ⚙️ Дополнительные настройки                  [X]  │
├─────────────────────────────────────────────────────┤
│                                                     │
│  📦 Модели                                          │
│  [Управление моделями...]                          │
│                                                     │
│  🔇 Фильтр тишины (VAD)                             │
│  [✓] Включён                                       │
│  Чувствительность: [═══●═══] Средняя               │
│                                                     │
│  ⚡ Производительность                              │
│  Размер буфера:     [═══●═══] 500 мс               │
│  (меньше = быстрее отклик, больше = меньше CPU)    │
│                                                     │
│  🚀 Запуск                                          │
│  [✓] Запускать при старте Windows                  │
│  [ ] Запускать свёрнутым                           │
│                                                     │
│  📊 Статистика                                      │
│  Слов за сегодня: 1,234                            │
│  Слов всего: 45,678                                │
│  [Сбросить статистику]                             │
│                                                     │
│              [Закрыть]                              │
└─────────────────────────────────────────────────────┘
```

**Затрагиваемые файлы:**
- `src/main.py` — полная переработка окна настроек

**Зависимости:**
- #16 Расширенное окно настроек (это его улучшенная версия)
- Все настраиваемые улучшения

**Критерии готовности:**
- [ ] Базовые настройки на первом экране
- [ ] Продвинутые скрыты за кнопкой
- [ ] Все надписи понятны без технического образования
- [ ] Есть предустановки качества
- [ ] Есть tooltips для сложных опций
- [ ] UI выглядит современно и аккуратно
- [ ] Работает на разных разрешениях экрана

---

## 📦 Новые зависимости

| Улучшение | Библиотека | Версия | Размер |
|-----------|------------|--------|--------|
| #9 Toast-уведомления | win11toast | >=0.34 | ~50 KB |
| #11 VAD | webrtcvad | >=2.0.10 | ~100 KB |

Добавить в `requirements.txt`:
```
win11toast>=0.34
webrtcvad>=2.0.10
```

---

## 🔗 Граф зависимостей

```
[2] Single-instance ─────────────────────────────┐
                                                 │
[1] Ротация логов                                │
                                                 │
[3] Ошибки микрофона ◄── [4] Reconnect           │
         │                    │                  │
         └────────────────────┼──► [8] Выбор     │
                              │      микрофона   │
                              │                  │
[5] Graceful shutdown ◄───────┴──────────────────┘
         │
         └──► [2] (освобождение mutex)

[6] Звуковая обратная связь

[9] Toast-уведомления ◄── [7] Индикатор загрузки
                     ◄── [3] Ошибки микрофона

[11] VAD ◄── [10] Анимация трея

[12] chunk_size

[14] Валидация конфига
[15] Атомарное сохранение

[18] Статистика ◄── [19] Tooltip в трее

[16] Расширенные настройки ◄── [20] Скачивание моделей
[17] Автозапуск                      │
                                     │
[7] Индикатор загрузки ◄── [21] Гибридная загрузка
[20] Скачивание моделей ◄──────────┘

[22] Простой UI ◄── [16] Расширенные настройки
              ◄── (все настраиваемые улучшения)
```
