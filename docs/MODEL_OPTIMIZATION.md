# Оптимизация моделей Vosk

## Доступные модели

| Модель | Размер | RAM | Качество | Рекомендуется для |
|--------|--------|-----|----------|-------------------|
| vosk-model-small-ru-0.22 | 45 МБ | ~300 МБ | Базовое | Слабые ПК, ноутбуки |
| vosk-model-ru-0.42 | 1.8 ГБ | ~2 ГБ | Высокое | Мощные ПК |

## Выбор модели

### Быстрая модель (small)
**Когда использовать:**
- Слабый компьютер (< 4 ГБ RAM)
- Ноутбук с экономией батареи
- Нужен быстрый отклик
- Диктовка простых текстов

**Характеристики:**
- Загрузка: 1-2 секунды
- CPU: низкая нагрузка
- Точность: ~85-90%

### Качественная модель (large)
**Когда использовать:**
- Мощный компьютер (8+ ГБ RAM)
- Важна точность распознавания
- Специализированная лексика
- Длинные диктовки

**Характеристики:**
- Загрузка: 10-30 секунд
- CPU: средняя нагрузка
- Точность: ~95-98%

## Параметры оптимизации

### VAD (Voice Activity Detection)

VAD фильтрует тишину, чтобы не нагружать модель.

| Уровень | Описание | CPU | Качество |
|---------|----------|-----|----------|
| 0 | Минимальная фильтрация | Выше | Лучше |
| 1 | Низкая | Средняя | Хорошее |
| 2 | Средняя (рекомендуется) | Ниже | Хорошее |
| 3 | Агрессивная | Минимальный | Может обрезать начала слов |

**Настройка в config.json:**
```json
{
  "vad": {
    "enabled": true,
    "aggressiveness": 2
  }
}
```

### Размер чанка (chunk_size)

Определяет, сколько аудио обрабатывается за раз.

| Значение | Отклик | CPU | Рекомендация |
|----------|--------|-----|--------------|
| 2000 | Очень быстрый | Высокий | Для real-time UI |
| 4000 | Быстрый | Средний | Баланс |
| 8000 | Стандартный | Низкий | Рекомендуется |
| 16000 | Медленный | Минимальный | Только для мощных моделей |

**Настройка в config.json:**
```json
{
  "audio": {
    "chunk_size": 8000
  }
}
```

## Предустановки качества

VoiceInput предлагает три предустановки в настройках:

### ⚡ Быстрое
- VAD aggressiveness: 3
- chunk_size: 8000
- Минимальная нагрузка CPU
- Подходит для слабых ПК

### ⚖️ Сбалансированное (по умолчанию)
- VAD aggressiveness: 2
- chunk_size: 8000
- Оптимальный баланс

### 🎯 Точное
- VAD aggressiveness: 1
- chunk_size: 4000
- Максимальное качество
- Требует больше ресурсов

## Гибридная загрузка

VoiceInput автоматически использует гибридную загрузку если обе модели скачаны:

1. **Старт**: Загружается маленькая модель (1-2 сек)
2. **Фон**: Большая модель загружается в фоне
3. **Переключение**: Автоматически переключается на большую модель

Это обеспечивает:
- Быстрый старт приложения
- Высокое качество после полной загрузки
- Без прерывания работы

## Ручная оптимизация model.conf

Для продвинутых пользователей: можно редактировать файл `model.conf` в папке модели.

### Уменьшение beam width
```
--beam=10.0
--max-active=3000
```

Меньше значения = меньше CPU, но хуже качество.

### Рекомендуемые значения

**Для слабых ПК:**
```
--beam=8.0
--max-active=2000
```

**Для мощных ПК:**
```
--beam=13.0
--max-active=7000
```

## Устранение проблем

### Высокая нагрузка CPU в тишине
1. Включите VAD: `"vad": {"enabled": true}`
2. Увеличьте aggressiveness до 2-3
3. Увеличьте chunk_size до 8000+

### Обрезаются начала слов
1. Уменьшите VAD aggressiveness до 1-2
2. Говорите чуть медленнее

### Медленный отклик
1. Используйте маленькую модель
2. Уменьшите chunk_size до 4000
3. Закройте другие программы

### Низкое качество распознавания
1. Используйте большую модель
2. Проверьте микрофон
3. Уменьшите VAD aggressiveness
