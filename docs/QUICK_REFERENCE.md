# ⚡ Краткая памятка VoiceInput

> Быстрый справочник для разработки и сборки

---

## 🔢 Версии — ГДЕ МЕНЯТЬ

| Файл | Что менять | Пример |
|------|------------|--------|
| `src/updater.py` | `CURRENT_VERSION = "X.X.X"` | `"1.0.5"` |
| `installer.iss` | `#define MyAppVersion "X.X.X"` | `"1.0.5"` |

**ВАЖНО:** Менять в ОБОИХ местах при каждом релизе!

---

## 🏗️ Сборка

```powershell
cd D:\cursor\audio

# 1. Собрать exe
python -m PyInstaller --clean build_exe.spec

# 2. Собрать установщик
& "D:\Program Files (x86)\Inno Setup 6\ISCC.exe" installer.iss

# Результат:
# - dist/VoiceInput.exe
# - output/VoiceInput-Setup-X.X.X.exe
```

---

## 📤 Релиз на GitHub

```powershell
# 1. Коммит
git add -A
git commit -m "v1.0.5: Описание изменений"
git push origin main

# 2. Создать release_notes.txt с описанием

# 3. Создать релиз
gh release create v1.0.5 "output/VoiceInput-Setup-1.0.5.exe" --title "VoiceInput v1.0.5" --notes-file release_notes.txt

# 4. Удалить временный файл
Remove-Item release_notes.txt
```

---

## 📁 Ключевые файлы

```
src/
├── main.py              # Главный класс VoiceInputApp
├── config.py            # Настройки (config.json)
├── audio_capture.py     # Захват с микрофона
├── speech_recognition.py # Vosk распознавание
├── hotkey_manager.py    # Горячие клавиши
├── updater.py           # Обновления ← CURRENT_VERSION
├── themes.py            # Тёмная/светлая тема
├── first_run.py         # Туториал
└── system_tray.py       # Иконка в трее

build_exe.spec           # PyInstaller ← hiddenimports
installer.iss            # Inno Setup ← MyAppVersion
```

---

## ➕ При добавлении нового модуля

1. Создать `src/new_module.py`
2. Добавить в `build_exe.spec`:
   ```python
   hiddenimports=[
       ...
       'new_module',  # ← добавить
   ]
   ```
3. Пересобрать exe

---

## ✅ Реализованные функции

| # | Функция | Версия |
|---|---------|--------|
| 23 | Туториал первого запуска | 1.0.1 |
| 24 | Автопроверка обновлений | 1.0.1 |
| 25 | Push-to-Talk режим | 1.0.1 |
| 26 | Исправление бага настроек | 1.0.1 |
| 27 | Инсталлятор Inno Setup | 1.0.1 |
| 28 | Кодировка микрофонов | 1.0.2-1.0.4 |
| 29 | Кнопка проверки обновлений | 1.0.3 |
| 30 | Улучшенное логирование | 1.0.3 |
| 31 | Тёмная тема | 1.0.5 |
| 32 | Автоустановка обновлений | 1.0.5 |
| 33 | Публичный репозиторий | 1.0.5 |

---

## ⏳ Запланированные (Волны 1-9)

```
Волна 1: Стабильность      Волна 2: Обработка ошибок
├── #1 Ротация логов       ├── #14 Валидация конфига
├── #2 Single-instance     ├── #15 Атомарное сохранение
└── #5 Graceful shutdown   ├── #3 Ошибки микрофона
                           └── #4 Reconnect

Волна 3: UX                Волна 4: Производительность
├── #9 Toast-уведомления   ├── #12 chunk_size → 8000
├── #6 Звуковая связь      └── #11 VAD фильтр
└── #7 Индикатор загрузки

Волна 5-9: См. IMPROVEMENTS.md
```

---

## 🔍 Отладка

```powershell
# Логи
Get-Content D:\cursor\audio\voice_input.log -Tail 50

# Проверка API обновлений
(Invoke-RestMethod -Uri "https://api.github.com/repos/NarekMan21/vosk/releases/latest").tag_name

# Статус репозитория
gh repo view NarekMan21/vosk --json visibility
```

---

## 🆘 Частые проблемы

| Проблема | Решение |
|----------|---------|
| EXE не запускается | Добавить в `hiddenimports` |
| Обновления 404 | Сделать репозиторий публичным |
| Настройки не открываются | Перезапустить приложение |
| "Иероглифы" микрофонов | `_fix_device_name()` |

---

## 📋 Чек-лист релиза

- [ ] Код работает (тест `python src/main.py`)
- [ ] Версия обновлена в `updater.py`
- [ ] Версия обновлена в `installer.iss`
- [ ] Новые модули в `hiddenimports`
- [ ] EXE собран и протестирован
- [ ] Установщик создан
- [ ] Документация обновлена
- [ ] Коммит и пуш
- [ ] Релиз на GitHub

---

*Версия: 1.0.5 | Обновлено: 17.01.2026*
