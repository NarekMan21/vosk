# 🎨 UI Редизайн VoiceInput

> **Цель:** Превратить убогий интерфейс в современный, лёгкий и красивый — как у Apple.

## Оглавление

1. [Общая концепция](#-общая-концепция)
2. [Зависимости](#-зависимости)
3. [Быстрые победы](#-быстрые-победы-5-минут-каждая)
4. [Основные улучшения](#-основные-улучшения)
5. [Дополнительные улучшения](#-дополнительные-улучшения)
6. [Порядок реализации](#-порядок-реализации)

---

## 🎯 Общая концепция

### Стиль: Apple-like Minimalism
- **Чистота** — минимум элементов, максимум "воздуха"
- **Глубина** — слоистые оттенки серого для визуальной иерархии
- **Акцент** — один яркий цвет для интерактивных элементов
- **Современность** — скруглённые углы, мягкие тени, плавные переходы

### Цветовая палитра (Тёмная тема)

```python
MODERN_DARK = {
    # Фоны (от тёмного к светлому)
    'bg_primary': '#0D0D0D',      # Подложка
    'bg_secondary': '#161618',    # Основной фон окна
    'bg_elevated': '#1C1C1E',     # Карточки, панели (приподнятые)
    'bg_hover': '#2C2C2E',        # При наведении
    
    # Текст
    'text_primary': '#FFFFFF',    # Основной
    'text_secondary': '#8E8E93',  # Второстепенный
    'text_muted': '#48484A',      # Приглушённый
    
    # Акцент (выбрать один)
    'accent': '#0A84FF',          # Apple Blue
    'accent_hover': '#0066CC',    # Темнее при наведении
    
    # Границы
    'border': '#38383A',
    'border_light': '#48484A',
    
    # Состояния
    'success': '#30D158',
    'warning': '#FFD60A',
    'error': '#FF453A',
}
```

### Типографика

```python
FONTS = {
    'heading': ('Segoe UI', 24, 'bold'),    # Заголовок окна
    'title': ('Segoe UI', 16, 'bold'),      # Заголовки секций
    'body': ('Segoe UI', 12, 'normal'),     # Основной текст
    'caption': ('Segoe UI', 10, 'normal'),  # Подписи, footer
}
```

### Отступы

```python
SPACING = {
    'xs': 4,
    'sm': 8,
    'md': 16,
    'lg': 24,
    'xl': 32,
}
```

---

## 📦 Зависимости

### Новый пакет: CustomTkinter

```bash
pip install customtkinter
```

**Зачем:** Современные виджеты со скруглёнными углами, красивыми переключателями, сегментированными кнопками.

**Обновить:** `requirements.txt`

---

## ⚡ Быстрые победы (5 минут каждая)

### UI-1. Новая цветовая палитра ✅

**Цель:** Заменить скучные серые цвета на современные многослойные оттенки.

**Что сделать:**
- Обновить `DARK_THEME` в `themes.py`
- Использовать near-black вместо чёрного
- Добавить разные оттенки для глубины

**Затрагиваемые файлы:**
- `src/themes.py`

**Критерии готовности:**
- [x] Фон окна: `#161618`
- [x] Фон карточек: `#1C1C1E`
- [x] Акцент: `#0A84FF`
- [x] Текст читаемый

---

### UI-2. Увеличенные отступы ✅

**Цель:** Добавить "воздуха" — интерфейс будет выглядеть премиальнее.

**Что сделать:**
- Увеличить padding в LabelFrame с 10 до 16
- Увеличить отступы между секциями с 5 до 12
- Увеличить padding кнопок

**Затрагиваемые файлы:**
- `src/main.py` (функция `open_settings`)
- `src/first_run.py` (туториал)

**Критерии готовности:**
- [x] Между секциями минимум 12px
- [x] Внутри секций padding 16px
- [x] Кнопки имеют padding 12px по горизонтали

---

### UI-3. Крупные заголовки ✅

**Цель:** Создать визуальную иерархию через типографику.

**Что сделать:**
- Заголовок окна: 24pt, bold
- Заголовки секций: 14pt, bold
- Основной текст: 12pt

**Затрагиваемые файлы:**
- `src/main.py`
- `src/first_run.py`

**Критерии готовности:**
- [x] "Настройки VoiceInput" — крупный заголовок
- [x] Секции визуально различаются от содержимого

---

### UI-4. Footer с версией ✅

**Цель:** Добавить "душевность" и профессиональный вид.

**Что сделать:**
- Добавить внизу окна: `VoiceInput v1.0.5 • Сделано с ❤️`
- Мелкий серый текст, выровнен по центру

**Затрагиваемые файлы:**
- `src/main.py`

**Критерии готовности:**
- [x] Footer виден внизу окна настроек
- [x] Версия берётся из `CURRENT_VERSION`
- [x] Текст серый, мелкий

---

### UI-5. Тёмные тултипы ✅

**Цель:** Заменить жёлтые Windows-тултипы на современные тёмные.

**Что сделать:**
- Изменить фон тултипа на `#2C2C2E`
- Текст белый
- Добавить скруглённые углы (через toplevel + overrideredirect)

**Затрагиваемые файлы:**
- `src/main.py` (функция `create_tooltip`)
- `src/themes.py` (функция `create_tooltip`)

**Критерии готовности:**
- [x] Тултипы тёмные с белым текстом
- [x] Тултипы появляются плавно (опционально)

---

## 🎨 Основные улучшения

### UI-6. Переход на CustomTkinter ✅

**Цель:** Современные виджеты вместо устаревших Tkinter.

**Что сделать:**
- Установить `customtkinter`
- Заменить `tk.Tk()` на `ctk.CTk()`
- Заменить `ttk.Button` на `ctk.CTkButton`
- Заменить `ttk.Checkbutton` на `ctk.CTkSwitch`
- Заменить `ttk.Combobox` на `ctk.CTkComboBox`
- Заменить `ttk.Radiobutton` на `ctk.CTkSegmentedButton`

**Затрагиваемые файлы:**
- `requirements.txt` — добавить `customtkinter`
- `src/main.py` — окно настроек
- `src/first_run.py` — туториал
- `src/updater.py` — диалог обновления
- `src/model_manager.py` — окно управления моделями

**Зависимости:**
- Нет (можно делать первым)

**Критерии готовности:**
- [x] CustomTkinter установлен
- [x] Окно настроек использует CTk виджеты
- [x] Кнопки скруглённые
- [x] Переключатели вместо чекбоксов
- [x] Всё работает без ошибок

---

### UI-7. Карточки вместо LabelFrame ✅

**Цель:** Заменить рамочки на современные карточки с тенями.

**Что сделать:**
- Создать функцию `create_card(parent, title)` в `themes.py`
- Карточка = CTkFrame с `corner_radius=12`, `fg_color='#1C1C1E'`
- Заголовок карточки — Label внутри, bold
- Заменить все LabelFrame на карточки

**Затрагиваемые файлы:**
- `src/themes.py` — функция `create_card`
- `src/main.py` — использовать карточки

**Зависимости:**
- UI-6 (CustomTkinter)

**Критерии готовности:**
- [x] Функция `create_card` работает
- [x] Все секции настроек — карточки
- [x] Карточки имеют скруглённые углы
- [x] Визуально отделяются от фона

---

### UI-8. Кнопки разных стилей ✅

**Цель:** Главная кнопка выделяется, второстепенная — нейтральная.

**Что сделать:**
- "Сохранить" — яркая (accent color), filled
- "Отмена" — прозрачная с обводкой (outline)
- Hover-эффекты для обеих

**Затрагиваемые файлы:**
- `src/main.py`

**Зависимости:**
- UI-6 (CustomTkinter)

**Критерии готовности:**
- [x] "Сохранить" — яркая синяя кнопка
- [x] "Отмена" — прозрачная с границей
- [x] При наведении цвет меняется

---

### UI-9. Сегментированный выбор качества ✅

**Цель:** Три радиокнопки заменить на один сегментированный контрол.

**Что сделать:**
- Использовать `CTkSegmentedButton` для выбора качества
- Варианты: "⚡ Быстрое", "⚖️ Баланс", "🎯 Точное"
- Один ряд вместо трёх строк

**Затрагиваемые файлы:**
- `src/main.py`

**Зависимости:**
- UI-6 (CustomTkinter)

**Критерии готовности:**
- [x] Выбор качества в одну строку
- [x] Выглядит как у Apple (сегменты)
- [x] Работает корректно

---

### UI-10. Toggle Switch вместо чекбоксов ✅

**Цель:** Современные переключатели как на iOS/macOS.

**Что сделать:**
- Все `ttk.Checkbutton` заменить на `CTkSwitch`
- Цвет активного состояния — зелёный (`#30D158`)

**Затрагиваемые файлы:**
- `src/main.py`
- `src/first_run.py`

**Зависимости:**
- UI-6 (CustomTkinter)

**Критерии готовности:**
- [x] Все чекбоксы — переключатели
- [x] Зелёный цвет когда включено
- [x] Плавная анимация переключения

---

## ✨ Дополнительные улучшения

### UI-11. Плавное появление окна (Fade-in) ✅

**Цель:** Окно появляется плавно, не резко.

**Что сделать:**
- При открытии окна — анимация прозрачности от 0 до 1
- Использовать `root.attributes('-alpha', value)`
- Цикл с `after()`, шаг 0.05, интервал 20мс

**Затрагиваемые файлы:**
- `src/main.py`

**Зависимости:** Нет

**Критерии готовности:**
- [x] Окно настроек появляется плавно (~300мс)
- [x] Нет мерцания
- [x] Работает на Windows 10/11

---

### UI-12. Hover-подсветка строк ✅

**Цель:** При наведении на настройку — строка подсвечивается.

**Что сделать:**
- Для каждой карточки — hover-эффект
- При `<Enter>` — менять bg на `#252528`, border на accent
- При `<Leave>` — возвращать оригинальные цвета

**Затрагиваемые файлы:**
- `src/main.py`

**Зависимости:** Нет

**Критерии готовности:**
- [x] При наведении карточка светлеет
- [x] Граница становится акцентной

---

### UI-13. Иконка статуса микрофона

**Цель:** Визуальный индикатор работы микрофона.

**Что сделать:**
- Рядом с комбобоксом микрофона — индикатор
- 🟢 — работает
- 🔴 — ошибка
- ⚪ — не выбран/не проверен

**Затрагиваемые файлы:**
- `src/main.py`

**Зависимости:** Нет

**Критерии готовности:**
- [ ] Индикатор виден
- [ ] Обновляется после теста микрофона
- [ ] Цвет соответствует состоянию

---

### UI-14. Улучшенная иконка трея

**Цель:** Более современная, тонкая иконка.

**Что сделать:**
- Уменьшить толщину линий микрофона
- Добавить лёгкий градиент фона
- Сгладить края (antialiasing)

**Затрагиваемые файлы:**
- `src/system_tray.py`

**Зависимости:** Нет

**Критерии готовности:**
- [ ] Иконка выглядит современнее
- [ ] Хорошо видна при любом фоне трея
- [ ] Анимация пульсации сохранена

---

### UI-15. Окно туториала — Welcome Screen ✅

**Цель:** Превратить туториал в красивый welcome screen.

**Что сделать:**
- Большой заголовок с иконкой
- Карточки для каждого шага
- Иллюстрации (эмодзи или простая графика)
- Кнопка "Начать" — большая и яркая

**Затрагиваемые файлы:**
- `src/first_run.py`

**Зависимости:**
- UI-6 (CustomTkinter)
- UI-7 (Карточки)

**Критерии готовности:**
- [x] Туториал выглядит как welcome screen
- [x] Шаги в карточках
- [x] Кнопка "Начать" акцентная
- [x] Fade-in анимация

---

## 📋 Порядок реализации

### Фаза 1: Быстрые победы (без новых зависимостей)

| # | Задача | Время | Приоритет |
|---|--------|-------|-----------|
| UI-1 | Новая цветовая палитра | 10 мин | 🔴 |
| UI-2 | Увеличенные отступы | 10 мин | 🔴 |
| UI-3 | Крупные заголовки | 5 мин | 🔴 |
| UI-4 | Footer с версией | 5 мин | 🟡 |
| UI-5 | Тёмные тултипы | 10 мин | 🟡 |

**Результат:** Интерфейс уже заметно лучше, без новых библиотек.

---

### Фаза 2: CustomTkinter

| # | Задача | Время | Приоритет |
|---|--------|-------|-----------|
| UI-6 | Установка CustomTkinter | 5 мин | 🔴 |
| UI-8 | Кнопки разных стилей | 15 мин | 🔴 |
| UI-10 | Toggle Switch | 20 мин | 🔴 |
| UI-9 | Сегментированный выбор | 15 мин | 🟡 |
| UI-7 | Карточки | 30 мин | 🟡 |

**Результат:** Современные виджеты, интерфейс как у Apple.

---

### Фаза 3: Полировка

| # | Задача | Время | Приоритет |
|---|--------|-------|-----------|
| UI-11 | Fade-in окна | 10 мин | 🟢 |
| UI-12 | Hover-подсветка | 15 мин | 🟢 |
| UI-13 | Статус микрофона | 10 мин | 🟢 |
| UI-14 | Иконка трея | 20 мин | 🟢 |
| UI-15 | Welcome screen | 30 мин | 🟢 |

**Результат:** Полированный, профессиональный интерфейс.

---

## 🔧 Технические заметки

### CustomTkinter — базовый шаблон

```python
import customtkinter as ctk

# Настройка темы
ctk.set_appearance_mode("dark")
ctk.set_default_color_theme("blue")

# Создание окна
root = ctk.CTk()
root.title("Настройки")
root.geometry("480x600")

# Кнопка
btn = ctk.CTkButton(
    root,
    text="Сохранить",
    corner_radius=8,
    height=40,
    fg_color="#0A84FF",
    hover_color="#0066CC"
)
btn.pack(pady=20)

# Switch
switch = ctk.CTkSwitch(
    root,
    text="Тёмная тема",
    progress_color="#30D158"
)
switch.pack(pady=10)

root.mainloop()
```

### Совместимость Tkinter + CustomTkinter

- CTk виджеты можно смешивать с обычными Tk
- `CTkToplevel` вместо `Toplevel` для дочерних окон
- Некоторые опции называются по-другому (`fg_color` вместо `bg`)

### Проверка после каждого изменения

```bash
python src/main.py
# Открыть настройки, проверить:
# - Окно открывается без ошибок
# - Все виджеты видны
# - Кнопки работают
# - Настройки сохраняются
```

---

## ✅ Чек-лист готовности

После реализации всех улучшений:

- [ ] Тёмная тема выглядит современно (не "Windows 2000")
- [ ] Есть визуальная иерархия (заголовки, карточки)
- [ ] Кнопки скруглённые, с hover-эффектами
- [ ] Переключатели вместо чекбоксов
- [ ] Достаточно "воздуха" между элементами
- [ ] Один акцентный цвет для интерактивных элементов
- [ ] Окно появляется плавно
- [ ] Туториал выглядит как welcome screen
- [ ] Нет ошибок при открытии/закрытии окон
- [ ] Настройки сохраняются корректно

---

*Документ создан: 17 января 2026*
*Версия: 1.0*
